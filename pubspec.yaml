name: startin
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.4.4 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  supabase_flutter: ^2.8.1
  image_picker: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_native_splash: ^2.2.20
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/Burger.jpg

flutter_native_splash:
  color: "#ffffff" # Warna background splash screen 
  image: assets/Burger.jpg # Path gambar splash screen
  android: true
  ios: true
  android:delay: 2000
